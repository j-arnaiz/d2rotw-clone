<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>D2 DClone Monitor</title>
    <style>
      body { font-family: monospace; background: #1a1a1a; color: #e0e0e0; margin: 0; padding: 20px; }
      h1 { color: #c8a84b; }
      a { color: #c8a84b; }
      table { border-collapse: collapse; width: 100%; max-width: 600px; }
      th, td { border: 1px solid #444; padding: 10px 16px; text-align: left; }
      th { background: #2a2a2a; color: #c8a84b; }
      .progress-6 { color: #ff4444; font-weight: bold; }
      .progress-5 { color: #ff8800; }
      .meta { color: #888; font-size: 0.85em; margin-top: 20px; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
